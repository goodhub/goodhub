{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start-all": "npx lerna run start --stream --parallel",
    "start": "cd packages/ui && npm run main:dev",
    "setup": "npx lerna bootstrap && npx lerna link"
  }
}
