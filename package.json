{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "npx lerna run start --stream --parallel",
    "setup": "npx lerna bootstrap"
  }
}
