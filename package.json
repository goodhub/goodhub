{
  "name": "root",
  "private": true,
  "type": "module",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "npx lerna run start --stream --parallel",
    "setup": "npx lerna bootstrap -- --legacy-peer-deps"
  }
}
